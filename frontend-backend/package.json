{
  "name": "frontend-backend-root",
  "version": "1.0.0",
  "scripts": {
    "backend": "npm run dev --prefix backend/API-TokoPakaian",
    "frontend": "npm run dev --prefix frontend",
    "dev": "concurrently --prefix \"[{name}]\" --prefix-colors \"cyan,magenta\" --names \"BACKEND,FRONTEND\" \"npm run backend\" \"npm run frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
